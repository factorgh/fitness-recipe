import 'package:flutter/material.dart';

class ReviewIcon extends StatelessWidget {
  const ReviewIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.star,
      color: Colors.yellow,
    );
  }
}
